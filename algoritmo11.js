
// Convertir un booleano en una cadena

// Implemente una función que convierta el valor booleano dado en su representación de cadena.

// Nota: Solo se darán entradas válidas.

function booleanToString(b){

    return b.toString()
    // return b ? 'true' : 'false'
   
    // return String(b);
  }

  console.log(booleanToString(true))
  console.log(booleanToString(false))

//   Interpretacion:
//   Uso el toString()método que devuelve un booleano como una cadena.
