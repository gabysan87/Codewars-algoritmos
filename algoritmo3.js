// Orden de lista inverso
// En este kata, creará una función que toma una lista y devuelve una lista en el orden inverso.

const reverseList = (list) => {

    return list.reverse()
}

console.log(reverseList([1,2,3,4]))
console.log(reverseList([3,1,5,4]))


// Interpretacion
// Uso el método reverse () que invierte el orden de los elementos de un array in place. El primer elemento pasa a ser el último y el último pasa a ser el primero.
